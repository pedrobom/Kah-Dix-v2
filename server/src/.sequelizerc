'use strict'

require('dotenv').config()

const path = require('path');

module.exports = {
    "config": path.resolve(__dirname, 'db', 'config', 'config.js'),
    "migrations-path": path.resolve(__dirname, 'db', 'database', 'migrations')
};