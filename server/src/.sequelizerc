'use strict'

require('dotenv').config()
const path = require('path');

module.exports = {
    "config": path.resolve(__dirname, 'POSTGRESQL', 'config', 'config.js'),
    "migrations-path": path.resolve(__dirname, 'POSTGRESQL', 'database', 'migrations')
};